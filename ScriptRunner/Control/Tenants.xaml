<Window x:Class="ScriptRunner.Control.Tenants"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Title="Tenants" Height="300" Width="600">
  

    <Grid Name="parent">
        <Grid.ColumnDefinitions>
            <!-- Contains the button -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <!-- So that we have the 'empty' space at the end -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions> 

    <Grid Name="_mainGrid" Margin="0,0,0,35" Grid.Column="0" Grid.Row="0">
        <Grid.ColumnDefinitions>
            <!-- Contains the button -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <!-- So that we have the 'empty' space at the end -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        

    </Grid>
        <Button Content="Update" HorizontalAlignment="right" Height="25" Margin="0,0,20,20" VerticalAlignment="bottom" Width="117" Click="btnUpdate_Click"/>
    </Grid>
</Window>
